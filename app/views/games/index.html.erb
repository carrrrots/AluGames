  <tbody>
  <div class="container-fluid">
	<div class="row">
		<div class="col-md-12">
			<nav class="navbar navbar-expand-lg navbar-light index-navbar row">
				<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <div class="col-2">
					<ul class="navbar-nav w-100 d-flex justify-content-end">
						<li class="nav-item dropdown">
							 <a class="nav-link dropdown-toggle  dropdown-category btn bg-light mr-2 rounded-pill shadow active" href="http://example.com" id="navbarDropdownMenuLink" data-toggle="dropdown"><i class="fa fa-align-justify"></i> Categories</a>
							<div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
								 <a class="dropdown-item" href="#">Action</a> <a class="dropdown-item" href="#">Another action</a> <a class="dropdown-item" href="#">Something else here</a>
							</div>
						</li>
					</ul>
          </div>
          <div class="col rounded-pill">
            <form class="form-inline">
              <div class="input-group w-100 group-active-shadow">
                <div class="input-group-prepend">
                  <span class="input-group-text shadow" id="basic-addon1"><i class="fas fa-search"></i></span>
                </div>
                  <%= form_tag games_path, method: :get do %>
                  <%= text_field_tag :query,
                    params[:query],
                    class: "form-control",
                    id: "search-bar",
                    placeholder: "What to play?"
                  %>
                  <% submit_tag %>
                <% end %>
              </div>
            </form>
          </div>
          <div class="col-3">
					<ul class="navbar-nav ml-md-auto mr-5">
						<li class="nav-item">
							 <a><%= link_to 'Alugar Jogo', new_game_path, class: "btn btn-primary d-flex justify-content-center rounded-pill mx-2" %></a>
						</li>
            <li class="nav-item">
							 <a><%= link_to 'Pedidos', games_path, class: "btn btn-secondary d-flex justify-content-center rounded-pill mx-2" %></a>
						</li>
					</ul>
          </div>
				</div>
			</nav>
		</div>
	</div>
</div>
<div class="card-container1">
<% @games.each do |game| %>
<div class="card m-4" style="width: 18rem; height:30%">
  <%if game.photo.present? %>
  <%= cl_image_tag game.photo.key, class: "card-img-top default-img", crop: :fill %>
  <% else %>
  <%= image_tag 'game.jpg', class: "card-img-top default-img"%>
  <% end%>
  <div class="card-body shadow">
    <h5 class="card-title"><%= game.name %></h5>
    <p class="card-category"><%= game.category %></p>
    <p class="card-description"><%= game.description %></p>
    <div class="d-flex justify-content-between">
    <p class="card-price">R$<%= game.price %>,00</p>
    <p class="card-players">Jogadores: <%= game.number_of_players %></p>
    </div>
    <p><%= link_to 'Show', game, class: "btn d-flex justify-content-center mx-5 mt-2 rounded-pill btn-show link-btn" %></p>
  </div>
</div>
<% end %>
</div>
